apiVersion: v1
kind: Service
metadata:
  name: {{ template "kyverno-http-authorizer-authz-server.name" . }}
  namespace: {{ template "kyverno.lib.namespace" . }}
  labels:
    {{- include "kyverno-http-authorizer-authz-server.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - name: http-auth
    port: {{ .Values.service.port }}
    protocol: TCP
    {{- with .Values.service.appProtocol }}
    appProtocol: {{ . }}
    {{- end }}
    targetPort: http-auth
    {{- if and (eq .Values.service.type "NodePort") (not (empty .Values.service.nodePort)) }}
    nodePort: {{ .Values.service.nodePort }}
    {{- end }}
  selector:
    {{- include "kyverno-http-authorizer-authz-server.labels.selector" . | nindent 4 }}
